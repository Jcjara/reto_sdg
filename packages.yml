packages:
  # Core utils: generic macros (union, surrogate keys, expression_is_true, etc.)
  - package: dbt-labs/dbt_utils
    version: ">=1.1.1"

  # Extra data quality tests (null %, ranges, regex, row count comparisons, etc.)
  - package: calogica/dbt_expectations
    version: ">=0.10.3"

  # Date/time helpers (e.g., generating calendars, shifting dates, etc.)
  - package: calogica/dbt_date
    version: ">=0.10.0"
